package com.orbits.feature.calendar

/**
 * UI model for calendar event (re-export from domain).
 * We keep it as a typealias for simplicity, but could add UI-specific extensions.
 */
typealias CalendarEvent = com.orbits.domain.calendar.CalendarEvent
